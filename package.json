{
  "name": "boilerplate-node-api",
  "version": "0.0.3",
  "main": "index.js",
  "description": "",
  "author": "Mateus Perdigão Domiciano",
  "keywords": [
    "A nice generator that creates an express application using clean architecture"
  ],
  "scripts": {
    "generate:new-api": "node ./index.js",
    "generate": "./generate-app.sh",
    "test": "BOILERPLATE_ENV='TEST' && chmod +x _test-generated-api.sh && ./_test-generated-api.sh",
    "preversion": "",
    "version:major": "npm version major --force -m 'Updating the project major version %s'",
    "version:minor": "npm version minor --force -m 'Updating the project minor version %s'",
    "version:patch": "npm version patch --force -m 'Updating the project patch version %s'",
    "postversion": "git push && git push origin --tags"
  },
  "dependencies": {
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2",
    "husky": "^4.2.5"
  }
}
